#include <stdio.h>
/**
 * main - begining
 * Return: Definetly 0 (success)
 */
int main(void)
{
	int n;
	int o;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (o = 97; o <= 102; o++)
	{
		putchar(o);
	}
	putchar('\n');
	return (0);
}
