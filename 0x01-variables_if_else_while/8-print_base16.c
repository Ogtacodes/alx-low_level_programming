#include <stdio.h>
/**
 * main - begining
 * Return: Definetly 0 (success)
 */
int main(void)
{
	int n;
	int o;

	for (n = 48; n <= 57; n++)
	{
		putchar(n);
	}
	for (o = 65; o <= 70; o++)
	{
		putchar(o);
	}
	putchar('\n');
	return (0);
}
